import tensorflow as tf


def identity(input_, param_1: str = None, param_2: int = None):
    """
    element: one single data point

    returns: a tf.tensor
    """
    # do preprocess
    # e.g. tf.image.resize
    # e.g. tf.reshape
    # e.g. etc.

    return input_
